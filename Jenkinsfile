pipeline {
    agent any
    stages {
        stage('Build') {
           bat 'npm install'
            steps {
                echo 'Building..'
                sh 'npm instrall'
            }
        }
    }
}